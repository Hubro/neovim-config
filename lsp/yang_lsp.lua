return {
  default_config = {
    cmd = { vim.env.HOME .. "/.local/lib/yang-lsp/bin/yang-language-server" },
    filetypes = { "yang" },
    root_markers = { "yang.settings" },
  },
  docs = {
    description = [[
  TODO
      ]],
  },
}
